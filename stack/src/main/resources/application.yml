spring:
  h2:
    console:
      enabled: true
      path: /h2     # (1) Context path
  datasource:
    url: jdbc:h2:mem:project     # (2) JDBC URL
  sql:
    init:
      schema-locations:
      data-locations:
logging:
  level:
    org:
      springframework:
        jdbc:
          core: TRACE
